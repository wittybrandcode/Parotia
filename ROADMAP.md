# 🗺️ Parotia — خارطة طريق التطوير (ROADMAP)

> **الإصدار الحالي:** 0.2.0 · **آخر تحديث:** أغسطس 2026 · **الاختبارات الحالية:** 236 اختباراً / 26 ملفاً (كلها خضراء)
>
> هذا المستند يحدد **المرحلتين الأولى والثانية** من تطوير Parotia بتنفيذ كامل ومفصّل،
> مع نظام تتبع جاهز لمتابعة التقدّم في كل مرحلة.

---

## 📊 لوحة التحكم العامة

| المؤشر | الحالة |
|---|---|
| **المرحلة 1 — الأساسات** | 🟢 2 / 4 مكتملة · 🗑️ 2 / 4 أُزيلت |
| **المرحلة 2 — تجربة المستخدم** | 🟢 3 / 4 مكتملة · 🗑️ 1 / 4 أُزيلت |
| **الإجمالي** | 🟢 5 / 8 مكتملة · 🗑️ 3 / 8 أُزيلت |

### دليل تتبع التنفيذ

يُستخدَم رمز حالة واحد لكل مرحلة/مهمة، ويُحدَّث في كل جلسة عمل:

| الرمز | المعنى |
|---|---|
| 🔴 `لم تبدأ` | لم يبدأ العمل عليها |
| 🟡 `قيد التنفيذ` | بدأت ويتم إنجازها حاليّاً |
| 🟢 `مكتملة` | أُنجزت بالكامل واجتازت الاختبارات |
| 🔵 `مؤجلة` | أُجلت عمداً مع سبب |

> **قاعدة الالتزام:** لا يُعتبر أي بند `مكتمل` إلا بعد اجتياز
> `npm run typecheck` + `npm run lint` + `npm run test` + `npm run build`.

---

# 🟢 المرحلة 1 — الأساسات (Foundations)

> الهدف: تحويل الأداة من "منظّف يدوي" إلى "منظّف ذكي" بإكمال الخدمات
> المُصمَّمة أصلّاً في البنية (Extraction / Mutation / Preset Engines).

## الحالة العامة للمرحلة

```
[x] المهمة 1.1 — Delete Similar (حذف المتشابهات)      🟢 مكتملة
[x] المهمة 1.2 — Smart Cleanup (التنظيف الذكي)         🗑️ أُزيلت بالكامل
[x] المهمة 1.3 — Site Presets (حفظ قواعد المواقع)      🟢 مكتملة
[x] المهمة 1.4 — HTML Export (تصدير HTML نظيف)         🗑️ أُزيلت بالكامل
```

---

## 1.1 — حذف المتشابهات (Delete Similar)

**الأولوية:** عالية · **الاعتماديات:** لا شيء · **الحالة:** 🟢 مكتملة

### الوصف
عند اختيار عنصر (إعلان، شريط جانبي...) يوفّر الزر "حذف المتشابهات" حذف كل
العناصر التي تطابق نفس البنية (CSS path) بنقرة واحدة.

### خطوات التنفيذ
1. **محرك المطابقة** — إنشاء `src/content/matching/matchEngine.ts`:
   - استخراج البنية البنائية للعنصر المحدد: `tagName` + تسلسل `class` (بترتيبها) + `data-*` الحاسمة.
   - دالة `findSimilar(ref): Element[]` تجمع عناصر نفس المستوى/الحاوية تحمل نفس التوقيع.
   - استبعاد العناصر المحمية (`data-newsclean-keep`) وواجهة NewsClean نفسها.
2. **أمر دفعة جديد** — إضافة `DELETE_MATCHING` (الموجود سلفاً في `messages.ts`):
   - تنفيذ الحذف عبر `MutationEngine` كـ `BatchCleanupOperation` **قابل للتراجع كوحدة واحدة** (Undo واحد يعيد الكل).
   - تحديث `CleanupState.removedCount` بعدد المحذوفات فعليّاً.
3. **واجهة الوصول** — تمرير `InspectorActionHandlers`:
   - إضافة `onDeleteSimilar?: (count: number) => void` وزر جديد (أيقونة Copies/ListFilter) في شريط العنصر.
   - العرض: "تم حذف N متشابه(ة)" في خانة التغذية الراجعة.
4. **التحقق من الصفحات الحية** — اختبار يدوي على موقع إخباري (قائمة أخبار متشابهة).

### الملفات المتأثرة
- `src/content/matching/matchEngine.ts` (جديد)
- `src/content/cleanup/cleanupEngine.ts` · `src/content/mutation/mutationEngine.ts`
- `src/content/inspector/inspector.ts` · `src/content/index.ts`
- `src/shared/types/messages.ts` (نوع `BatchCleanupOperation` موجود)

### معايير القبول (DoD)
- [x] 6+ اختبارات جديدة في `tests/content/matching/matchEngine.test.ts` (8 اختبارات)
- [x] التراجع (Undo) يعيد كل عناصر الدفعة في مكانها وترتيبها
- [x] العناصر المحمية لا تُحذف أبداً
- [x] إجمالي الاختبارات ≥ 69 (74 اختباراً)

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-14 | إنشاء `DefaultMatchEngine` (توقيع بنيوي: tag + classes + data-*) مع استبعاد المحمي وواجهة NewsClean | 🟢 |
| 2026-08-14 | `MutationEngine.deleteSimilar` كأمر دفعة واحد قابل للتراجع (يعيد الترتيب) | 🟢 |
| 2026-08-14 | `CleanupEngine.deleteSimilarTargets` + تفعيل `DELETE_MATCHING` وزر "Delete similar" في شريط العنصر | 🟢 |

---

## 1.2 — التنظيف الذكي (Smart Cleanup)

**الأولوية:** عالية · **الاعتماديات:** 1.1 · **الحالة:** 🗑️ أُزيلت بالكامل

### الوصف
زر واحد "تنظيف ذكي" يحلّل الصفحة عبر `ExtractionEngine` (جاهز) ويزيل تلقائياً
العناصر المصنّفة **إعلان / نافذة اشتراك / شريط جانبي / وسائل تواصل** بدرجة ثقة ≥ عتبة،
مع عرض معاينة لما سيتغيّر قبل التأكيد.

### خطوات التنفيذ
1. **قناة الاقتراحات** — استكمال `cleanup.propose()` وربط نتائج `extraction.run()`.
2. **واجهة معاينة** — لوحة صغيرة في شريط الأداة تعرض الاقتراحات:
   - كل صف: معاينة نص + الفئة + درجة الثقة + مربع اختيار.
   - أزرار: "تأكيد الكل" · "تأكيد المحدد" · "إلغاء".
3. **التنفيذ الآمن** — المرور عبر `MutationEngine` كعملية دفعة واحدة قابلة للتراجع.
4. **الحدود** — لا يتمس أبداً: الجسم الرئيسي للمقال، العناوين، والعناصر المحمية.

### الملفات المتأثرة
- `src/ui/src/App.tsx` + `src/ui/src/styles.css` (لوحة المعاينة)
- `src/content/cleanup/cleanupEngine.ts` · `src/content/extraction/extractionEngine.ts`
- `src/shared/types/messages.ts` (أمر `SMART_CLEANUP` أو توسعة `APPLY_PRESET`)

### معايير القبول
- [x] 4+ اختبارات لقناة الاقتراحات (7 اختبارات: فرز، فلترة، حد الثقة، تطبيق، إلغاء، حماية)
- [x] إلغاء التعديل لا يغيّر DOM
- [x] العنصر الرئيسي للمقال محمي دائماً
- [x] التراجع يستعيد العدادات الصحيحة لكل دفعة (`affectedCount` على الأمر)

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-14 | `junkDiscovery.ts` — اكتشاف الفوضى (إعلانات/كوكيز/نشرة/شريط جانبي/روابط) بعتبة ثقة 60 | 🗑️ |
| 2026-08-14 | `propose()` + `applyProposals()` — قناة اقتراحات مع فرز وتفريغ التداخل وحماية المقال | 🗑️ |
| 2026-08-14 | `MutationEngine.deleteMany(refs, source)` + `affectedCount` على الأوامر لتراجع صحيح للدفعات | 🗑️ |
| 2026-08-14 | لوحة معاينة في الشريط: قائمة بالفئة والدرجة ومربعات اختيار + تطبيق المحدد/إلغاء (زر Sparkles) | 🗑️ |
| 2026-08-15 | **إزالة كاملة للميزة**: حذف `junkDiscovery.ts` + `propose`/`applyProposals` من `CleanupEngine` + نوع `SmartProposal` و `proposalCount` + أمر `SMART_CLEANUP` من allowlist/التوجيه/المعالج + زر Smart ولوحة المعاينة من الشريط + أنماط `nc-prop-*` + 8 اختبارات. السبب: لا تعمل بشكل موثوق على الصفحات الحية، وستُبنى مستقبلاً بنسخة متقدمة تستخدم الذكاء الاصطناعي | 🟢 |

---

## 1.3 — قواعد المواقع (Site Presets)

**الأولوية:** متوسطة · **الاعتماديات:** 1.1 · **الحالة:** 🟢 مكتملة

### الوصف
حفظ قواعد التنظيف لكل موقع في `chrome.storage.sync` بحيث تُعاد الزيارة للموقع
المرّة القادمة بنفس التنظيف، مع تفعيل/تعطيل من لوحة الخيارات.

### خطوات التنفيذ
1. **نموذج البيانات** — اعتماد `CleanupRule[]` + `SitePreset` (المعرّف في `PRESET-SYSTEM.md`):
   - `{ site, rules: CleanupRule[], enabled, updatedAt }` مخزّنة بالمفتاح `newsclean.presets`.
2. **التخزين** — خدمة `storage/presetStore.ts` (قراءة/كتابة/دمج) على `chrome.storage.sync`.
3. **التطبيق** — في `START_SESSION`: إذا وُجد Preset للموقع الحالي و`enabled` → تطبيق القواعد عبر `MutationEngine` (دفعة قابلة للتراجع).
4. **الواجهة** — أزرار في الشريط: "حفظ قواعد هذا الموقع" + مؤشر "قاعدة نشطة ✓".
5. **صفحة الخيارات** — `options/` لإدارة القواعد (تفعيل/تعطيل/حذف).

### الملفات المتأثرة
- `src/storage/presetStore.ts` (جديد) · `src/shared/types/presets.ts`
- `src/content/index.ts` · `src/ui/src/App.tsx` · `src/ui/options.html`
- `scripts/build-manifest.mjs` (خيارات + إذن `storage`)

### معايير القبول
- [x] 4+ اختبارات لـ `presetStore` (حفظ/دمج/تطبيق) — 12+ اختباراً (مخزن + مُطبِّق + مطابقة)
- [x] التراجع يلغي القواعد المطبّقة من الـ Preset (دفعة واحدة + عداد صحيح)
- [x] 2-3 مواقع افتراضية جاهزة (مثال حي) — CNN / BBC / Al Jazeera
- [x] **لا يفرض نفسه عنوة**: اكتشاف + زر تفعيل/تعطيل، والتطبيق التلقائي مرتبط بموافقة صريحة

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-14 | `CleanupEngine.applyPreset()` — قواعد الحذف دفعة واحدة قابلة للتراجع + قواعد الحماية (keep) + تخطّي الآمن | 🟢 |
| 2026-08-14 | `defaultPresets.ts` — 3 قواعد مدمجة (CNN/BBC/Al Jazeera) باختيارات متحفّظة و `required:false` | 🟢 |
| 2026-08-14 | `SAVE_PRESET` — حفظ قواعد الموقع من الشريط (كتابة عبر مخزن مُتحقق منه) + `APPLY_PRESET` اليدوي | 🟢 |
| 2026-08-14 | التطبيق التلقائي في `START_SESSION` عند وجود Preset مطابق + حالة `preset` في البث | 🟢 |
| 2026-08-14 | الشريط: زر "Save" + مؤشر "Preset ✓" باسم القاعدة النشطة | 🟢 |
| 2026-08-14 | صفحة الخيارات (`ui/options.html`): قائمة القواعد + تفعيل/تعطيل + حذف (vite متعدد الصفحات) | 🟢 |
| 2026-08-14 | بوابة خضراء كاملة (112 اختباراً) + build بثلاث مداخل (content/background/ui) | 🟢 |
| 2026-08-14 | **تفعيل اختياري**: `enabled` على مستوى النمط — لا يُطبَّق تلقائياً إلا بموافقة صريحة + زر Enable/Disable في الشريط + تبديل في صفحة الخيارات + حفظ يحافظ على `enabled` عند التحديث (114 اختباراً) | 🟢 |

---

## 1.4 — تصدير HTML نظيف (HTML Export)

**الأولوية:** متوسطة · **الاعتماديات:** — · **الحالة:** 🗑️ أُزيلت بالكامل

### الوصف
بدل الاقتصار على PNG، تصدير **نسخة HTML** من المقال بعد التنظيف: صالحة للقراءة
والنشر، مع إزالة السكربتات والإعلانات والأنماط غير الضرورية.

### خطوات التنفيذ
1. **استخراج المقال** — استخدام `extraction.run()` لتحديد حاوية المقال.
2. **التنظيف** — إزالة `script`/`link`/`iframe`/`style` غير الضرورية + السطوع التلقائي `data-*`.
3. **التضمين** — إنتاج مستند مستقل: `<html>` + خطوط النظام + أنماط صغيرة مضمّنة.
4. **الحفظ** — `Blob` → `chrome.downloads.download` (نفس نمط `downloadPng`).

### الملفات المتأثرة
- `src/content/export/htmlExporter.ts` (جديد)
- `src/background/service-worker.ts` (أمر `EXPORT` موجود في البنية)
- `src/ui/src/App.tsx` (زر تصدير)

### معايير القبول
- [x] 4+ اختبارات لوحدة التصدير (إزالة السكربتات، تضمين النص، تسمية الملف) — 11 اختباراً
- [x] الملف الناتج يُفتح محليّاً ويعرض المقال نظيفاً (مستند مستقل + خطوط نظام + CSS مضمّن)
- [x] اسم الملف مأخوذ من العنوان مع تطهير الأحرف الممنوعة

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-14 | `htmlExporter.ts` — `cleanArticleClone` (إزالة script/style/link/iframe/form/svg + سمات data-*/on*/style) | 🗑️ |
| 2026-08-14 | `buildStandaloneDocument` — doctype + charset + viewport + عنوان مُهرَّب + CSS مضمّن (خطوط نظام، صور متجاوبة) | 🗑️ |
| 2026-08-14 | `exportFilename` — تطهير أحرف ويندوز الممنوعة + ضبط الحجم | 🗑️ |
| 2026-08-14 | `downloadHtmlDocument` — Blob + blob URL (تجاوز سقف data URL) → `chrome.downloads.download` | 🗑️ |
| 2026-08-14 | **إصلاح**: `chrome.downloads` غير متاح في content scripts — نُقل التنزيل إلى الـ Service Worker (أمر `DOWNLOAD_HTML` جديد + `src/background/downloads.ts` عبر data URL، نفس نمط PNG) | 🗑️ |
| 2026-08-14 | ربط معالج `EXPORT` في content: استخراج المقال عبر `extraction.run()` (أو body كبديل) + تنظيف + تنزيل | 🗑️ |
| 2026-08-14 | زر "HTML" في الشريط + رسائل تعليق أثناء/بعد التصدير | 🗑️ |
| 2026-08-14 | بوابة خضراء (125 اختباراً) + build كامل | 🗑️ |
| 2026-08-15 | **إزالة كاملة للميزة**: حذف `htmlExporter.ts` + `src/background/downloads.ts` + اختباراتها، وإزالة أمرَي `EXPORT`/`DOWNLOAD_HTML` من allowlist/التوجيه/المعالج + زر "HTML" ورسائله من الشريط. السبب: فكرة الإضافة هي الحصول على صورة PNG نظيفة بعد التنظيف لا الحصول على صفحة HTML | 🟢 |

---

# 🟡 المرحلة 2 — تجربة المستخدم (UX)

> الهدف: رفع مستوى الانسيابية والراحة اليومية للمستخدم أثناء القراءة والتنظيف.

## الحالة العامة للمرحلة

```
[x] المهمة 2.1 — اختصارات لوحة المفاتيح            🟢 مكتملة
[x] المهمة 2.2 — وضع القراءة (Reading Mode)          🗑️ أُزيلت بالكامل (ليست من غرض الإضافة)
[x] المهمة 2.3 — لوحة السجل (Action Log)             🟢 مكتملة
[x] المهمة 2.4 — تصدير العنصر المحدد كصورة           🟢 مكتملة
```

---

## 2.1 — اختصارات لوحة المفاتيح

**الأولوية:** عالية · **الاعتماديات:** — · **الحالة:** 🟢 مكتملة

### الوصف
تشغيل أهم الأوامر من الكيبورد دون لمس الشريط.

### خطوات التنفيذ
1. **خريطة الاختصارات** (بدون تعارض مع اختصارات المتصفح):
   - `Shift+Alt+F` — تجميد/إلغاء تجميد الصفحة
   - `Shift+Alt+P` — بدء/إيقاف الالتقاط (Pick)
   - `Escape` — إيقاف الالتقاط وإغلاق شريط العنصر
   - `Delete` / `Backspace` — حذف العنصر المحدد (عندما يكون الالتقاط نشطاً)
   - `Enter` — تأكيد الاقتراح المميّز في لوحة Smart Cleanup
2. **المعالج** — `src/content/keyboard/shortcuts.ts` يستمع على `keydown` مع كشف الحالة
   (freeze FROZEN + inspecting) ويستدعي نفس `handleCommand` (اتساق كامل).
3. **الحماية** — تجاهل عند الكتابة في حقول الإدخال/النصوص.
4. **الدليل** — تلميحات الاختصارات في تلميحات الأزرار (`title`).

### الملفات المتأثرة
- `src/content/keyboard/shortcuts.ts` (جديد) · `src/content/index.ts`
- `src/ui/src/App.tsx` (تلميحات)

### معايير القبول
- [ ] 4+ اختبارات (توزيع الاختصارات، تجاهل حقول الإدخال، Escape)
- [x] 4+ اختبارات (توزيع الاختصارات، تجاهل حقول الإدخال، Escape) — 6 اختبارات
- [x] لا تعارض مع `Ctrl+Shift+*` المتصفحية — فقط `Shift+Alt` + منع `Ctrl/Cmd`

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-14 | `src/content/keyboard/shortcuts.ts` — معالج `keydown` capture مع كشف الحالة (frozen/inspecting/selection) | 🟢 |
| 2026-08-14 | نفس `handleCommand` تماماً للاختصارات (اتساق كامل مع الشريط والتراجع) | 🟢 |
| 2026-08-14 | حماية حقول الإدخال (`input/textarea/select/contenteditable`) + تجاهل `Ctrl/Cmd` | 🟢 |
| 2026-08-14 | تلميحات الاختصارات على أزرار الشريط (Freeze/Pick/Delete) | 🟢 |
| 2026-08-14 | 6 اختبارات + بوابة خضراء كاملة (89 اختباراً) | 🟢 |

---

## 2.2 — وضع القراءة (Reading Mode)

**الأولوية:** متوسطة · **الاعتماديات:** — · **الحالة:** 🗑️ أُزيلت بالكامل

### الوصف
تحويل الصفحة مؤقّتاً إلى تجربة قراءة مريحة: عمود مركزي، خط أكبر، خلفية هادئة —
دون حذف أي محتوى (قابل للعكس الكامل).

### خطوات التنفيذ
1. **تحديد الحاوية** — استخراج العنصر الرئيسي للمقال (حسب `ExtractionEngine`).
2. **التحويل البصري** — فئة `reading-mode` على الحاوية:
   - `max-width: 70ch` + توسيط، `font-size` أكبر، `line-height 1.7`، لون خلفية حسب الثيم.
3. **الإخفاء الجانبي** — `visibility: hidden` لكل ما ليس داخل الحاوية (مؤقّت، غير مسجّل في السجل).
4. **التراجع** — زر "خروج من وضع القراءة" يستعيد كل الأنماط (لا يمسّ السجل ولا الـ Undo).

### الملفات المتأثرة
- `src/content/reading/readingMode.ts` (جديد) · `src/content/index.ts`
- `src/ui/src/App.tsx` + `styles.css` (زر BookOpen/Type)

### معايير القبول
- [x] 3+ اختبارات (تفعيل/تعطيل، حفظ الأنماط الأصلية، الإخفاء الجانبي)
- [x] الخروج يعيد الصفحة كما كانت حرفيّاً

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-15 | `src/shared/types/reading.ts` — `ReadingState { status: "OFF"/"ON", articleSelector }` | 🗑️ |
| 2026-08-15 | `src/content/reading/readingMode.ts` — `DefaultReadingEngine` (تفعيل/تعطيل/حالة): يحدد الحاوية عبر `ExtractionEngine.run()`، يضيف `<style>` مُداراً + وسمي `data-newsclean-reading` على `html` و `data-newsclean-article` على الحاوية، يخفي كل ما خارج الحاوية بـ `visibility: hidden` مع إبقاء شريط NewsClean ظاهراً، ويعيد الصفحة حرفيّاً عند الخروج دون لمس السجل/الـ Undo | 🗑️ |
| 2026-08-15 | أوامر `READING_START`/`READING_STOP` (allowlist + توجيه SW) + معالجة في content + `reading` في `broadcastState`/`getSnapshot` | 🗑️ |
| 2026-08-15 | زر "Read/Exit Read" (أيقونة BookOpen) في الشريط + رسائل ملاحظات | 🗑️ |
| 2026-08-15 | 8 اختبارات (تفعيل، إخفاء جانبي مع إبقاء الشريط، استعادة حرفية، استعادة قيمة وسم سابقة، عدم لمس DOM عند غياب مقال، تكرار آمن، حاوية = body، خروج من OFF) | 🗑️ |
| 2026-08-15 | بوابة خضراء (139 اختباراً / 20 ملفاً) + build كامل | 🗑️ |
| 2026-08-15 | **إزالة كاملة للميزة**: حذف `src/content/reading/` و `src/shared/types/reading.ts` و `tests/content/reading/` + إزالة الأوامر من allowlist والتوجيه ومعالجات content وحالة `reading` من `broadcastState`/`getSnapshot` وزر الشريط (الإضافة بُنيت للتنظيف والتصدير لا للقراءة) | 🟢 |

---

## 2.3 — لوحة السجل (Action Log)

**الأولوية:** متوسطة · **الاعتماديات:** — · **الحالة:** 🟢 مكتملة

### الوصف
قائمة قابلة للطي تعرض كل إجراءات الجلسة (حُذف/أُخفي/حُفظ/قُطعت لقطة) مع نقر للتراجع عن أي إجراء فردي.

### خطوات التنفيذ
1. **مصدر البيانات** — `HistoryEngine` (محرك التراكم في الـ content) أصبح مصدر السجل مباشرة:
   - `history.log()` يبني `ActionLogEntry[]` **الأحدث أولاً** (id + label + at + undoable) من مكدّسي التراجع/الإعادة.
   - `history.peekUndo()` يكشف رأس مكدّس التراجع، و`history.undoTo(entryId)` يتراجع المتدرّج حتى الإجراء الهدف.
2. **الحالة** — إضافة `actionLog: ActionLogEntry[]` (نوع جديد في `session.ts`) إلى `broadcastState` و`getSnapshot`.
3. **الواجهة** — زر History في الشريط يفتح لوحة منسدلة تحت الشريط:
   - صف لكل إجراء: نص + وقت نسبي ("just now / 3m ago") + زر تراجع فردي (يعمل فقط على الإجراءات غير المُتراجع عنها).
   - التراجع الفردي ينفّذ أمر `UNDO_TO` الجديد (allowlist + توجيه SW) → `cleanup.undoThrough(entryId)` (Undo خطوة بخطوة مع عدادات صحيحة حتى الهدف).
4. **التحديث اللحظي** — يُعاد بث السجل مع كل `broadcastState`.

### الملفات المتأثرة
- `src/content/mutation/history.ts` (كشف السجل + `peekUndo`/`undoTo`) · `src/content/cleanup/cleanupEngine.ts` (`undoThrough`)
- `src/content/mutation/mutationEngine.ts` (`canUndo`/`peekUndo`) · `src/content/index.ts`
- `src/shared/types/session.ts` · `src/shared/types/messages.ts` (أمر `UNDO_TO`) · `src/background/service-worker.ts`
- `src/ui/src/App.tsx` + `styles.css` (زر History + لوحة السجل)

### معايير القبول
- [x] 3+ اختبارات (بناء السجل، التراجع الفردي) — 6 اختبارات سجل + اختبار `undoThrough`
- [x] السجل يختفي مع انتهاء الجلسة (حالة الشريط تُعاد إلى فارغة عند غياب الجلسة)

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-14 | `HistoryEngine` — مكدّسات تحمل `{ command, at }` + `log()` (الأحدث أولاً مع `undoable`) + `peekUndo()` + `undoTo()` | 🟢 |
| 2026-08-14 | `DefaultCleanupEngine.undoThrough()` — تراجع متدرّج عبر `undo()` حتى الإجراء الهدف مع عدادات صحيحة | 🟢 |
| 2026-08-14 | أمر `UNDO_TO` (allowlist + توجيه SW) + `actionLog` في `broadcastState`/`getSnapshot` | 🟢 |
| 2026-08-14 | لوحة السجل: زر History + صفوف (نص + وقت نسبي + تراجع فردي) في الشريط | 🟢 |
| 2026-08-14 | بوابة خضراء (131 اختباراً) + build كامل | 🟢 |
| 2026-08-15 | **ربط أزرار Undo/Redo بالسجل**: إضافة زر **Redo** للشريط + تفعيل/تعطيل Undo/Redo من حالة مكدّسات الـ History (`canUndo`/`canRedo`) بدل عدد المحذوفات + تلميح يعرض الإجراء الذي سيُتراجع عنه/يُعاد | 🟢 |

---

## 2.4 — تصدير العنصر المحدد كصورة (Element Capture)

**الأولوية:** منخفضة · **الاعتماديات:** — · **الحالة:** 🟢 مكتملة

### الوصف
إضافة أيقونة كاميرا داخل شريط العنصر لتصدير **العنصر المحدد فقط** كصورة PNG عالية الدقة.

### خطوات التنفيذ
1. **الوضع الجديد** — توسيع `CAPTURE` ليشمل `mode: "ELEMENT"` + `elementId`:
   - لفّ العنصر في حاوية معزولة، إخفاء الباقي، ثم نفس مسار `captureVisibleArea`.
2. **الدقة** — `devicePixelRatio` لتكبير واضح.
3. **الحفظ** — إعادة استخدام `downloadPng`.

### الملفات المتأثرة
- `src/shared/types/messages.ts` · `src/background/service-worker.ts`
- `src/content/inspector/inspector.ts` (زر كاميرا في شريط العنصر)

### معايير القبول
- [x] 2+ اختبارات (تحضير العنصر، إخفاء الباقي، الاستعادة)
- [x] لا يظهر شريط العنصر نفسه في الصورة

### ✅ سجل التنفيذ
| التاريخ | الإجراء | الحالة |
|---|---|---|
| 2026-08-15 | `src/content/capture/elementCapture.ts` (جديد) — `ElementCaptureIsolator` (عزل مرئي عكوس: `<style>` مُدار يخفي كل أبناء body عدا العنصر المُوسوم + يخفي `data-newsclean-root`، تمرير العنصر إلى أعلى الشاشة، واستعادة دقيقة) + `cropRegion` (حساب نطاق القص بدقة الجهاز dpr) + `cropToPng` (قص المشهد الملتقط إلى حدود العنصر وإعادة ترميزه) | 🟢 |
| 2026-08-15 | توسيع أمر `CAPTURE` بـ `mode: "ELEMENT"` + `elementId` وأوامر `PREPARE_ELEMENT_CAPTURE`/`CAPTURE_ELEMENT_CROP`/`CAPTURE_ELEMENT_RESTORE` (allowlist + توجيه SW) | 🟢 |
| 2026-08-15 | `captureElement` في SW: إخفاء الشريط → عزل العنصر → `captureVisibleTab` → قص في content (مخزّن في `chrome.storage.local`) → `downloadPng` ثم استعادة كاملة في `finally` | 🟢 |
| 2026-08-15 | زر كاميرا في شريط العنصر (inspector) يعمل فقط عند توفّر `onCapture` ويرسل CAPTURE إلى الـ SW مباشرة | 🟢 |
| 2026-08-15 | 7 اختبارات (قص بامتداد dpr، قص حدودي، عنصر بلا أبعاد، عزل + إخفاء الباقي، إخفاء شريط العنصر، استعادة حرفية، إعادة عزل لعنصر آخر) | 🟢 |
| 2026-08-15 | بوابة خضراء (146 اختباراً / 21 ملفاً) + build كامل | 🟢 |
| 2026-08-15 | إعادة تسمية وهوية بصرية: **Parotia** + شعار `Parotia.svg` + slogan "clean the stage. keep the story." — تحديث manifest (الاسم/العنوان/الوصف)، شريط الأدوات (شعار طائر + Parotia)، صفحة الخيارات، عناوين الصفحات، أسماء ملفات التصدير (`parotia-*.png`)، حقل author للبريستات | 🟢 |
| 2026-08-15 | الهوية اللونية للشعار: حاوية خضراء فاتحة `#dcf2c6` + أيقونة سوداء `#0f141a` (لأن طائر الـ Parotia أسود) في شريط الأدوات وصفحة الخيارات + favicon مطابق (`src/ui/favicon.svg` → `assets/favicon-*.svg`) لصفحتي toolbar و options | 🟢 |
| 2026-08-15 | أيقونة شريط المتصفح (action icon): `scripts/generate-icons.mjs` (sharp) يولّد PNG (16/32/48/128) من `src/ui/favicon.svg` إلى `public/icons/` + `dist/icons/`، وأُضيف `icons` + `action.default_icon` إلى `build-manifest.mjs` و`public/manifest.json`، وأُدرجت خطوة `build:icons` في سلسلة الـ build | 🟢 |
| 2026-08-15 | دمج زرّي Freeze/Unfreeze في زر تبديل واحد (label/icon يتبدلان حسب الحالة + `nc-btn-active` عند التجميد). عند التجميد: الشعار يتحول لأخضر قاتم `#166534` + الطائر أسود، والطائر داخل الحاوية (وليس الحاوية) يدور عكس عقارب الساعة بشكل مستمر `infinite` ما دام التجميد مفعلاً كدلالة على استمرار العمل | 🟢 |
| 2026-08-15 | توحيد اللون الأخضر `#C1E899` في كل الحالات (شريط الأدوات + صفحة الخيارات + favicon + أيقونات الإضافة)، وعند التجميد/الرقص تُضاف توهّج نابض (`parotia-glow`) للحاوية الخضراء | 🟢 |
| 2026-08-15 | كتابة اسم العلامة "PAROTIA" بأحرف كبيرة وبلون أخضر `#C1E899` بحجم أكبر، وحذف سهم إخفاء/إظهار الأيقونات (إزالة `collapsed` والزر وCSS الخاص به) | 🟢 |
| 2026-08-15 | جعل الشعار + كلمة PAROTIA هما زرّ تبديل التجميد (حذف زر Freeze/Unfreeze المخصص). النقر على منطقة العلامة يجمّد الصفحة فوراً، والنقرة الثانية تطلقها؛ مع مؤشرات إفوردنس (pointer + hover + focus-visible + aria-pressed) | 🟢 |
| 2026-08-15 | إصلاح تصوير المكوّن المحدد: (1) وضع علامة `data-newsclean-capture` على عنصر `<html>` أيضاً (كانت على العنصر فقط فلم تنطبق أي قاعدة عزل). (2) تصوير العنصر عبر مقاطع viewport وخياطتها بالـ stitcher بدل قصّ لقطة واحدة، فيتم تصوير العناصر الأطول من الشاشة كاملة (صورة التويتة لم تعد تُقتطع). (3) تمرير مسبق عبر مدى العنصر لتحميل الصور الكسولة وانتظار اكتمالها قبل الالتقاط. (4) إزالة `CAPTURE_ELEMENT_CROP` واستبداله بـ SCROLL/SLICE/FINALIZE | 🟢 |
| 2026-08-15 | تحسين تصوير المكوّن: (1) قصّ أفقي للصورة الملتَقطة إلى حدود العنصر فقط في `CAPTURE_ELEMENT_FINALIZE` لإزالة الفراغ الأيمن/الأيسر (مثل التويتة في عمود جانبي). (2) رفع الجودة لمكوّنات الصور فقط عبر تكبير مؤقت للتبويب ×2 أثناء الالتقاط (`chrome.tabs.setZoom`) — كل CSS px يغطي ضعف البكسلات — مع التراجع التلقائي للدقة الأصلية إذا تجاوز الارتفاع حدّ الكانفاس | 🟢 |
| 2026-08-15 | إصلاح تصوير تويتة خاطئة بعد اختيار تويتة ثانية: كان `stableSelector` ينتج `[data-testid="tweet"]` غير فريد، فيعود `querySelector` دائمًا لأول تويتة في DOM. أُعيدت كتابته ليختبر كل مرشّح (id/testid/class) مع شرط التطابق الفريد مع العنصر نفسه ويسقط إلى مسار `nth-of-type` البنيوي الفريد. كما يصلح هذا إخفاء/حذف التويتة المحددة نفسها (كانت تعمل على الأولى) | 🟢 |
| 2026-08-15 | نقاء صورة الالتقاط: (1) إخفاء طبقات المُلتقط نفسه (`[data-newsclean-highlight]` و `.nc-action-bar`) في CSS العزل — كانت معلّقة على `<html>` فأفلتت من قاعدة `body > *` وظهرت كإطار أزرق وشريط أزرار داخل الصورة. (2) تحميل إلزامي للصور (`loading="eager"`) داخل العنصر مع إعادتها كما كانت بعد الالتقاط + توقفات 120ms بين خطوات التمرير المسبق حتى تبدأ الصور الكسولة (صورة البروفايل والميديا) بالتحميل وتظهر كما هي تمامًا | 🟢 |
| 2026-08-15 | إصلاح عناصر مخفية في التصوير (صورة بروفايل وغيرها على X/Twitter): السبب أن المواقع تستخدم `content-visibility: auto` على عناصر الخلاصة فيُسقط المتصفح رسم الأجزاء خارج الشاشة. أُضيف `content-visibility: visible !important` للعنصر الملتقط ولكل أحفاده، وأُعيدت كتابة `waitForElementRendering` لتدفع كل صورة ناقصة عبر `img.decode()` مع فحص دوري حتى تكتمل فعليًا بدل انتظار `load` الذي قد لا يحدث أبدًا للصورة الكسولة | 🟢 |
| 2026-08-15 | إصلاح تصوير صورة البروفايل نفسها: (1) معالجة الحالة التي يكون فيها العنصر المحدد هو `<img>` نفسه (كان `querySelectorAll("img")` لا يشمل العنصر نفسه فلا يُفرض تحميله ولا يُنتظَر). (2) المواقع تخفي الوسائط عبر `opacity:0` على الحاوية الأم حتى اكتمال التحميل — يُفرض الآن رفع الشفافية `opacity:1` على الأسلاف الشفافة تمامًا (في JS لأن CSS لا يستطيع استهداف الأسلاف) مع الإعادة بعد الالتقاط، وقاعدة `opacity:1 !important` للعنصر نفسه. (3) سجل تشخيصي `[parotia] element capture prepared` في `console.debug` | 🟢 |

---

# 📌 ملاحظات ختامية

### تسلسل التنفيذ المقترح
```
1.1 → 1.2 → 2.1 → 1.3 → 1.4 → 2.3 → 2.2 → 2.4
```

### قواعد الجودة
1. **اختبار أولاً**: كل مهمة تبدأ بكتابة/تعديل اختباراتها.
2. **بوابة خضراء**: لا انتقال لمهمة قبل نجاح `typecheck + lint + test + build`.
3. **التوثيق**: تحديث قسم "سجل التنفيذ" لكل مهمة عند إتمامها.
4. **تحديث الحالة**: تغيير الدائرة الملونة في أعلى كل مهمة أولاً ثم اللوحة العامة.

### لوحة الحالة العامة (تُحدَّث دوريّاً)
| # | المهمة | الأولوية | الحالة |
|---|---|---|---|
| 1.1 | Delete Similar | عالية | 🟢 |
| 1.2 | Smart Cleanup | عالية | 🗑️ |
| 1.3 | Site Presets | متوسطة | 🟢 |
| 1.4 | HTML Export | متوسطة | 🗑️ |
| 2.1 | اختصارات الكيبورد | عالية | 🟢 |
| 2.2 | وضع القراءة | متوسطة | 🗑️ |
| 2.3 | لوحة السجل | متوسطة | 🟢 |
| 2.4 | Element Capture | منخفضة | 🟢 |
